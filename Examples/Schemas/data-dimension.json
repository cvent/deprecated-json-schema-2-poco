{
  "$schema": "http://json-schema.org/draft-03/schema#",
  "title": "DataDimension",
  "description": "Information about a dimension returned by a data-source",
  "type": "object",

  "javaType": "com.cvent.board.core.DataDimension",
  "csharpType": "com.cvent.board.core.DataDimension",

  "properties": {
    "label": {
      "description": "dimension label",
      "type": "string"
    },
    "type": {
      "enum": [
        "WidgetFilter",
        "WidgetGrouping",
        "DashboardFilter"
      ],
      "default": "WidgetGrouping",
      "description": "What this dimension should be used for in the ui."
    },
    "config": {
      "type": "object",
      "description": "config information required by data source impl. TODO."
    },
    "resultKey": {
      "description": "look up key in data row for this dimension",
      "type": "string",
      "default": "dimension1"
    }
  }
}
