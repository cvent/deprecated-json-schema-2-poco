{
  "schema": "http://json-schema.org/draft-03/schema#",
  "title": "DataMeasure",
  "description": "Information about a measure returned by a data-source",
  "type": "object",
  "properties": {
    "label": {
      "description": "A measure's ui label",
      "type": "string",
      "default": "New Measure"
    },
    "type": {
      "enum": [
        "Integer",
        "Float",
        "String",
        "UnixTimeStamp"
      ],
      "default": "Integer",
      "description": "Some type information about the measure"
    },
    "config": {
      "type": "object",
      "description": "config information required by data source impl. TODO."
    },
    "resultKey": {
      "description": "look up key in data row for this measure",
      "type": "string",
      "default": "value"
    }
  }
}
